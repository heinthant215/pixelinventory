<h1>Product List</h1>
<% products.forEach(product => { %>
  <div>
    <h2><%= product.name %></h2>
    <p>Price: $<%= product.price %></p>
    <p>Description: <%= product.description %></p>
  </div>
<% }) %>
